# Dominant terms
- Use only the largest power of the terms 
$$n^2+n=O(n^2)$$
# Formal Definition
The Asymptotic Upper Bound of a function f(n) using large values of n

# Problems with Big-O
Upper Bound only
If f(n) is O(n) it is also $O(n^2),O(n^3),O(n^4)$ for sufficiently large n


