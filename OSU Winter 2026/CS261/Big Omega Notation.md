Big $\Omega$ Notation
- Best case/Lower Bound
- if f(n) O(n^2) it is also $\Omega$(n), $\Omega$(log n), $\Omega$(1)
	- 3n is technically $\Omega$(1)
- Sometimes Big-O and Big-$\Theta$ are equal
	- finding the min val in an unsorted array
	- summing all the numbers in an arr